module FoedusML.Compression
    ( compress
    , decompress
    ) where

compress :: [Int] -> [Int]
compress = id  -- Aquí implementas el algoritmo real de compresión

decompress :: [Int] -> [Int]
decompress = id  -- Aquí implementas el algoritmo real de descompresión

